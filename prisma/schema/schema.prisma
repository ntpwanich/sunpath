// ไม่มีฟิลด์เวลาใดในไฟล์ชุดนี้ใช้ค่าตั้งต้นจากฐานข้อมูล ทุกค่าเขียนมาจากชั้น application
// เพราะ test และ script สร้างข้อมูลตัวอย่างต้องปักเวลาย้อนหลังเองได้
//
// ค่าที่คำนวณจากค่าอื่นได้ก็ไม่เก็บเช่นกัน (ขนาดระบบประเมิน มูลค่าคาด เส้นตาย
// เวลาที่ติดต่อครั้งแรก จำนวนวันที่ค้างอยู่ใน Stage) เพราะค่าที่เก็บซ้ำจะเพี้ยนเงียบ ๆ

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
